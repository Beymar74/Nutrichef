import 'package:flutter/material.dart';

class TopChefSection extends StatelessWidget {
  const TopChefSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // ya no muestra nada
  }
}
